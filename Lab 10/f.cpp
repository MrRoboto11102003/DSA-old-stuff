#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main()
{
    int n;
    cin>>n;
    vector<int> A;
    vector<int> B;
    int x;
    for(int i=0;i<n;i++)
    {
        cin>>x;
        A.push_back(x);
        B.push_back(x);
    }
    sort(A.begin(),A.end());

    int s=0;
    for(int i=0;i<n;i++)
    {

    }
}